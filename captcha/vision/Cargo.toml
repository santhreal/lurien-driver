[package]
name = "lurien-vision"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
description = "Gap detection helper for the slider challenge kind. Sees one crop, never the page."
authors = ["Santh <64453045+santhreal@users.noreply.github.com>"]
repository = "https://github.com/santhreal/lurien-driver"
publish = false

[[bin]]
name = "lurien-vision"
path = "src/main.rs"

[dependencies]
png = "0.17"
serde = { workspace = true }
serde_json = { workspace = true }
